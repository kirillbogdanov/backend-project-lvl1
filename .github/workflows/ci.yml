name: ci
on: push
jobs:
  test:
     name: test
     runs-on: ubuntu-18.04
     steps:
       - name: ls
         run: ls -la
       - name: Run install
         run: make install
       - name: Run linter
         run: make lint

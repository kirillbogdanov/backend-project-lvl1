name: ci

on: push

jobs:
  test:
    name: test
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        run: npm ci
      - name: Run linter
        run: make lint
